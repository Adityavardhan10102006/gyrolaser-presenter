# 3D-Laser-Pointer-Presentation-Tool
A 3D `PresentationBoard` component that renders slide textures with realistic material properties,A custom `LaserPointer` system that projects a glowing cursor onto the board surface by calculating intersections between the camera ray and the 3D geometry
